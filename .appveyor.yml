image: Ubuntu

stack: jdk 11

branches:
  only:
    - master

build: off

install:
    chmod 777 ./artifacts/app-order.jar
  - java -jar ./artifacts/app-order.jar

build_script:
  - chmod +x gradlew
  - ./gradlew test --info